import Image from "next/image";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Spin, spin, spiiiiiin",
  description: "Spin",
};

export default function Home() {
  return (
    <div>
	spin, spin, spiiiiin
    </div>
  );
}
